# PURPOSE: Assembles the system + user prompt from reranked chunks, injecting citations and instructions
# CALLED BY: gateway.query_handler
# DEPENDS ON: ingestion.metadata_schema (ChunkMetadata type)
