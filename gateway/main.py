# PURPOSE: FastAPI application entry point — registers routes and configures middleware
# CALLED BY: uvicorn (uvicorn gateway.main:app)
# DEPENDS ON: fastapi, gateway.query_handler, gateway.sse_streamer
